Tourism Marketplace Platform – Spec & Scaffold
Scope

We are building a tourism marketplace platform with the following components:

    Backend (FastAPI + SQLModel)

    Web Admin Panel (Next.js + React + TS)

    Providers Panel Web Frontend (Next.js + React + TS)

⚠️ The Mobile App for tourists will be implemented later, not in scope now.
Core Concept

The app connects tourism providers (companies offering trips) with tourists (end users who book trips).
Functional Requirements
Provider Onboarding

    Providers register via the Providers Panel Web Frontend.

    They must supply:

        User credentials: email, password, name, phone.

        Company credentials: name, email, phone, documents, logo.

        Multiple documents allowed → store in JSONB.

        company_metadata = Column(
            "metadata", JSONB, nullable=True
        )  # {company_registration_file: s3_path, logo: s3_path, ...}

    Creates:

        New user (initial provider user).

        New provider request (pending approval).

Admin Approval

    Admins handle provider requests in the Web Admin Panel.

    Can:

        Approve → provider company created, initial user becomes Super Provider User.

        Deny → denial reason must be provided (visible to provider).

Roles
Provider Roles

    Super Provider User

        Invite/delete provider users.

        Edit company profile.

        Manage trips fully.

    Provider User

        Manage trips.

        Cannot invite/delete users or edit company profile.

Admin Roles

    Super Admin

        Full access: manage providers, trips, users.

        Can invite/delete admin users.

    Admin User

        Same as Super Admin, except cannot invite/delete admin users.

Trips

    Providers can create trips with:

        Images (upload → S3).

        Title, description.

        Start/end datetime.

        Pickup location (Google Maps).

        Start city/country.

        Destination cities/countries.

        Accommodation included (boolean).

    Trips include:

        Subscribers (normal users who pay).

        Reviews (from subscribed users only).

    Cancellation Rule:

        No cancellation allowed within 2 days before start.

        If canceled earlier → refund all subscribers.

Normal Users (Tourists – via Mobile App later)

    Register with email or phone.

        Email → verification via SendGrid/Mailgun (feature flag via env).

        Phone → dummy OTP for now.

    Actions:

        Browse trips, filter by: start city, destination, start date, duration.

        View provider profiles (image, name, description, list of trips).

        Join trips (pay via Checkout.com sync flow).

        Leave reviews (only for joined trips).

        Manage profile (reset password, update name/email/phone).

        Cannot leave trips after joining.

Technical Requirements
Backend (FastAPI, Python 3.12)

    Stack: FastAPI, SQLModel, PostgreSQL, Alembic, Redis, Pydantic, Poetry.

    Features:

        Pytest unit tests for all endpoints.

        Mock 3rd-party integrations:

            S3 (trip images, company docs).

            Checkout.com (payments/refunds).

            SendGrid/Mailgun (emails).

            OTP (dummy).

        Localization (English only at start, but extensible).

        JSONB storage for flexible company metadata.

Frontends (Next.js + React + TS)

    Web Admin Panel

    Providers Panel

    Features:

        Role-based access control.

        CRUD for trips, companies, users.

        Status tracking for provider requests.

        Invitation system (email-based).

Infrastructure

    Each project must be dockerized separately.

    Each should run standalone with docker compose up.

    Root-level docker-compose.yml runs all 3 together.

Folder Structures
1. Backend (FastAPI)

backend/
│── alembic/                # DB migrations
│── app/
│   ├── api/
│   │   ├── routes/         # All API endpoints
│   │   ├── deps.py         # Dependencies (auth, pagination, etc.)
│   │   └── __init__.py
│   ├── core/               # Configs, security, utils
│   │   ├── config.py
│   │   ├── security.py
│   │   └── localization.py
│   ├── crud/               # DB queries
│   ├── models/             # SQLModel classes
│   ├── schemas/            # Pydantic schemas
│   ├── services/           # Business logic (emails, payments, s3, otp)
│   ├── tests/              # pytest tests
│   └── main.py             # FastAPI entrypoint
│── poetry.lock
│── pyproject.toml
│── Dockerfile
│── docker-compose.yml

2. Web Admin Panel (Next.js + TS)

admin-panel/
│── public/
│── src/
│   ├── components/
│   ├── hooks/
│   ├── layouts/
│   ├── pages/
│   ├── services/           # API calls
│   ├── store/              # State management
│   ├── utils/
│   └── styles/
│── tests/                  # Jest/RTL tests
│── tsconfig.json
│── package.json
│── Dockerfile
│── docker-compose.yml

3. Providers Panel (Next.js + TS)

providers-panel/
│── public/
│── src/
│   ├── components/
│   ├── hooks/
│   ├── layouts/
│   ├── pages/
│   ├── services/           # API calls for providers
│   ├── store/
│   ├── utils/
│   └── styles/
│── tests/
│── tsconfig.json
│── package.json
│── Dockerfile
│── docker-compose.yml

4. Root Project

project-root/
│── backend/
│── admin-panel/
│── providers-panel/
│── docker-compose.yml      # runs all 3 together

Testing

    Backend: pytest (mock external services).

    Frontend: Jest + React Testing Library.

Tech Stack Summary

Backend

    FastAPI

    SQLModel

    PostgreSQL

    Alembic

    Redis

    Pydantic

    Poetry

    Python 3.12

Frontend

    Next.js

    React

    TypeScript (latest stable)

👉 Please scaffold all 3 projects with the above folder structures, add minimal boilerplate files (main.py, config.py, _app.tsx, etc.), and ensure each project builds & runs with Docker.


instructuion set 2:

1- i see type errors in the typescript code, please fix them.
2- please add frontend pages for admin-panel and providers-panel. forntend should use tailwindcss 


instructuion set 3:

1- please add login pages to both admin-panel and providers-panel. 
2- please make sure that both forntends are protected by authentication. 
3- please add normal user mobile app user backend routes like login, register, forgot password, reset password, logout, profile, trips, join trip, leave trip, rate trip
4- there seems to be an error in admin-panel tsconfig.json file, please fix it

instructuion set 4:

1- create registration for providers panel
2- create unit tests for all endpoints. these unit tests shoudl call all endpoints and test them. if the endpoint has 3rd party calls then mock them
4- ready all 3 projects for being pushed to github. Meaning add .gitignores to all 3 projects, make sure secrets are not pushed as well but an example.env is pushed instead...
5- i don't think all admin panel pages redirect to login when user isn't logged in. fix that if it's true
6- if providers panel user isn't logged in, they should just see a register and login page. The login form is just email and password. But the register page is the page where the provider creates the provider creation request along with initial user credentials


instructuion set 5:

1- add source header for all requests in the backend so that we can know if a requests originates from app or admin panel or providers panel. also add an enum for it
2- backend: user phone number and email should be unique per source. so a user with 1 phone number can register for app and admin panel and providers panel with same phone number and that would count as 3 users. but they can't register for the providers panel twice with the same number. this same applies to emails as well. so if i register with phone number in app and then add my email and verify it then i woun't be able to use either the phone number or the email for another account in the mobile app source
3- both frontends: fix api call routes. for example: for login, the admin frontend calls /auth/login but the backend route is /api/v1/login/access-token. so the admin frontend should call /api/v1/login/access-token. also, all routes currently are prefixed /api/v1
4- backend: please complete any endpoint that is left hanging in the backend like the /reset-password endpoint has a comment # Verify token and reset password logic here. or for the /logout endpint it says # In a real-world application, you would invalidate the token. # For this example, we'll just return a success message. but add the logic for invalidating the token which should be adding it to a block list in redis until the refresh token for it expires. these 2 are just examples you should look for similiar comments and complete them
5- backend: log users out on password reset and password change
6- backend: update unit tests for these new additions
7- add .env files for all 3 projects and populate them with, currently there are only .env.eample files



instruction set 6:

1- backend: I got psycopg2.errors.UndefinedColumn: column user.source does not exist error. Please fix it and make sure you always run migration and test endpoits when you change anything
2- backend: for logout, I changed my mind, please delete the current logout implementation and remove any mentioned of it including unit tests. just leep the logout route and put a comment that it needs to be implemented
3- backend: for get_request_source, please fon't edault to any source if source header is not provided. make the  header required
4- both frontend: make sure admin panel and admin providers send the header source in the requests
5- update any needed unit tests from the mentioend changes


instruction set 7:

1- backend: add columns is_phone_verified and is_email_verified to user. these columns should be false by default.
2- backend: provider feature: when a provider creates a trip. they can chose which fields that any user joining the trip must fill or provide. the fields options are pre determined in code, but the provider can choose which ones will be shown to joining users in the app to fill. the fields are id/iqama number(text), passport number(text), name(text), phone(text), email(text), address(text), city(text), country(text), date of birth(date), gender(enum), disability(enum), medical conditions(text), allergies(text). the mobile app must have way of knowing that these are the required fields for a trip. either the backend provides a dedicated route for the app to know the required fields for a trip or the required fields are retunred with in /trips/{trip_id}   
2- backend: trip enhancement: add trip packages, so that a trip can have multiple packages with different prices. a package has a name and a price and a description.
3- backend: trip enhancement: a single mobile app user can register multiple people to a trip. this enhancement ties in with the required fields for a trip. when a user registers multiple people to a trip, they must provide the required fields for each person. 
4- add unit tests for new features and update unit tests for old affected features by these changes


instruction set 8:
1- backend: trip flow adjustment: a trip provider should be able to define required fields per trip package. not all packages need to have the same required fields. for exampl. 1 trip package migth be for adults so it requires a phone number and the other for children so it doesn't require a phone number
2- after making the needed adjustment please bring unit tests up to date with it. and also please make requests to the backend to make sure the flow is correct and everything works


instruction set 9:
1- providers panel: please bring providers panel to the latest provider chnages in the backend. including trip packages and packages fields.
2- backend and providers panel: please make sure that a provider adds at least one package to the trip. backend and provider panel should prevent this.
3- providers panel: please make sure if a provider creates a trip that there's a detailed page for it in the providers panel. where the provder can see trip packages and packages fields. 