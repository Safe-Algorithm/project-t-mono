from app.models.trip_field import TripFieldType, GenderType, DisabilityType

# This file now only contains enums and metadata - no schemas needed for TripRequiredField
# since fields are now managed at the package level via TripPackageRequiredField
